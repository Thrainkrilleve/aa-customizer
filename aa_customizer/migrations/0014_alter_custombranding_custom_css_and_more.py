# Generated by Django 6.0.3 on 2026-03-23 00:32

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('aa_customizer', '0013_login_page_custom_code'),
    ]

    operations = [
        migrations.AlterField(
            model_name='custombranding',
            name='custom_css',
            field=models.TextField(blank=True, help_text="CSS injected into every page via an inline &lt;style&gt; block, loaded after the active theme so it can override any style. Works alongside Alliance Auth's built-in Custom CSS admin.", verbose_name='Custom CSS'),
        ),
        migrations.AlterField(
            model_name='custombranding',
            name='custom_css_url',
            field=models.URLField(blank=True, help_text='URL of an external CSS stylesheet linked in every page &lt;head&gt; (e.g. a CDN-hosted custom theme file). Loaded after the active AA theme.', verbose_name='Custom CSS — URL'),
        ),
        migrations.AlterField(
            model_name='custombranding',
            name='head_extra_html',
            field=models.TextField(blank=True, help_text='Raw HTML injected at the very end of &lt;head&gt; on every page. Useful for analytics scripts, font imports, or custom meta tags. Only editable by admins — content is rendered without sanitization.', verbose_name='Extra <head> HTML'),
        ),
        migrations.AlterField(
            model_name='custombranding',
            name='login_page_body_html',
            field=models.TextField(blank=True, help_text='Raw HTML injected just before &lt;/body&gt; only on the login page. Useful for custom overlays, particle effects, animation scripts, or any markup/JavaScript needed for a fully custom landing page. Only editable by admins — content is rendered without sanitization.', verbose_name='Login Page — Extra Body HTML'),
        ),
        migrations.AlterField(
            model_name='custombranding',
            name='login_page_head_html',
            field=models.TextField(blank=True, help_text='Raw HTML injected at the end of &lt;head&gt; only on the login page. Useful for loading custom fonts, icon libraries, JS frameworks, or Open Graph tags specific to the login page. Only editable by admins — content is rendered without sanitization.', verbose_name='Login Page — Extra <head> HTML'),
        ),
    ]
