# Generated by Django 6.0.3 on 2026-03-22 21:27

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('aa_customizer', '0012_login_background_filefield'),
    ]

    operations = [
        migrations.AddField(
            model_name='custombranding',
            name='login_page_body_html',
            field=models.TextField(blank=True, help_text='Raw HTML injected just before </body> only on the login page. Useful for custom overlays, particle effects, animation scripts, or any markup/JavaScript needed for a fully custom landing page. Only editable by admins — content is rendered without sanitization.', verbose_name='Login Page — Extra Body HTML'),
        ),
        migrations.AddField(
            model_name='custombranding',
            name='login_page_css',
            field=models.TextField(blank=True, help_text='CSS injected only on the login page, after all other stylesheets. Has the highest specificity of any CSS on the login page — use it to completely override the card layout, colors, fonts, or any element.', verbose_name='Login Page — CSS'),
        ),
        migrations.AddField(
            model_name='custombranding',
            name='login_page_css_url',
            field=models.URLField(blank=True, help_text='URL of an external stylesheet linked only on the login page. Loaded after the global Custom CSS URL, so it takes full priority. Use this to load a completely different design framework (e.g. Tailwind, a custom theme CDN link) without affecting the rest of the site.', verbose_name='Login Page — CSS URL'),
        ),
        migrations.AddField(
            model_name='custombranding',
            name='login_page_head_html',
            field=models.TextField(blank=True, help_text='Raw HTML injected at the end of <head> only on the login page. Useful for loading custom fonts, icon libraries, JS frameworks, or Open Graph tags specific to the login page. Only editable by admins — content is rendered without sanitization.', verbose_name='Login Page — Extra <head> HTML'),
        ),
    ]
